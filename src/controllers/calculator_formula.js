// console.log(Object.keys(require('../models')));
const CalculatorFormula = require('../models').calculator_formula;

const Sequelize = require('sequelize');
const Op = Sequelize.Op

module.exports = {

}